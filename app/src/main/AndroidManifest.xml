<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="id.gits.gitsmvvmkotlin">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:name=".GitsApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppThemeDetail">
        <meta-data
            android:name="io.fabric.ApiKey"
            android:value="261e27dc5baae72193fd64e849234ca4bcf6971d" />

        <activity android:name=".mvvm.login.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".mvvm.messages.MessagesActivity" />
        <activity android:name=".mvvm.messagedetail.MessageDetailActivity" />
        <activity android:name=".mvvm.userprofile.UserProfileActivity" />
        <activity android:name=".mvvm.messagecompose.MessageComposeActivity"></activity>
    </application>

</manifest>